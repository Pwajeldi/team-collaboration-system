import type { UserList } from "../../types/types"

type ViewUserProfileProps = {
    user: UserList
}

const ViewUserProfile = ({}: ViewUserProfileProps) => {
    return(
        <div className="user-profile-container">

        </div>
    )
}

export default ViewUserProfile